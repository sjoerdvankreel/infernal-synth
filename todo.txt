* for each release
- x create version branch
- x update hiir
- x update vst3 sdk
- x update version number in code
- x update version number in cmake unless only bugfix release
- x gcc build
- x diff presets
- x update history
- x debug run demos
- x run pluginval
- x run the vst validator
- x keep manual up to date
- x don't forget to tag

* host bugs
- renoise/flstudio automation serialization is broken

* wishlist small
- buy reaper
- static linked version
- load/save patch option
- new menu option: clear all unused
- make more presets + group by folder

* wishlist big
- CLAP support
- osc types: feedback lead
- filter types: formant, vowel
- noise types: fractal, perlin
- fx types: compressor, phaser/flanger/chorus
- scale CV amount by global CV param
- improve current noise generator (1/fs)
- improve k+s generator (allow more stretching)
- improve dsf generator: high partials roll off too fast + allow falloff to the left

* wishlist ui
- tabbed ui
- larger knobs
- remove voice/master graphs
- reduce the use of background images
- drop text edit fields & put active knob value in 1 single place
- filter out incompatible menu options (osc sync, ram, audio routing)
- provide an alternate way to pop up the context menu or integrate it in the plugin ui

# programmatic ui
- get rid of uidesc
- drop uigenerator tool
- make project dependency diagram
- remove uidescription linker dependency
- move dependent visibility code to base ui project
- reduce controller.uidesc to image names, image offsets and color scheme
- drop view_container_fix etc (anything that's only there for uidescription binding)